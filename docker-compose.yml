#
# docker-compose build && docker-compose push
#
version: '3.5'
services:
  api-resume:
    build:
      context: .
      args:
        - NPM_TOKEN=7636bd9b-678c-40e2-8cb2-06dcddb8fa26
    image: newsolution/api-resume:latest
